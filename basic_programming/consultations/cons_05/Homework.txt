Сделать рефакторинг приложения в результате которого:

1. Должно быть два варианта ввода книг на полки (через userInput и через массив который задается внутри кода)
Ввод этих данных сделать отдельным классом и методом (не в main)

В результате у вас должен появится экземпляр класса Books в котором есть массив, содержащий все ваши книги

2. Заполнение содержимом полок сделать рандомно из массива, который находится в классе Books

3.* проверить наименование переменных и методов на повторение.
При необходимости переименовать

4* Перенести методы (при необходимости создать новые классы) исходя из того
 чем метод занимается и должен ли он находится там где сейчас находится